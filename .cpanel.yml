---
deployment:
  tasks:
    - export PATHROOT=/home/stmb5393/sim-skripsi/
    - /bin/cp -R app $PATHROOT
    - /bin/cp -R bootstrap $PATHROOT
    - /bin/cp -R config $PATHROOT
    - /bin/cp -R database $PATHROOT
    - /bin/cp -R public $PATHROOT
    - /bin/cp -R resources $PATHROOT
    - /bin/cp -R routes $PATHROOT
    - /bin/cp -R tests $PATHROOT
    - /bin/cp -R vendor $PATHROOT
